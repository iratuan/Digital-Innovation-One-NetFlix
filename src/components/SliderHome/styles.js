import styled from "styled-components";
import { wLg } from "../../assets/css/variaveis";

const Wrapper = styled.section`
  border: none;
  border-collapse: collapse;
  margin: -36px auto 0;
`;

export { Wrapper };
